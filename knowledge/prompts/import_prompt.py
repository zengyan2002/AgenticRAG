"""
导入相关的提示词模板管理
"""

# 文档主题名提示词模板
THEME_NAME_SYSTEM_PROMPT = """你是一个专门用于文档主题识别的信息抽取专家。
你的唯一任务是从用户提供的文档标题和文档片段中，精准提取出该文档主要描述的【核心主题名】。

【主题名定义】
主题名是这篇文档主要围绕的对象或事项名称。它可能是商品、设备、型号、系统、模块、算法、模型、数据集、标准、规范、项目、合同事项、报告主题或教程主题。

【提取规则】
1. 准确性：优先提取文档中明确出现、最能代表全文主题的名称，不要提取泛泛的类别词。
2. 完整性：如果文档中同时出现品牌、系列、型号、对象类别，请尽量组合成完整名称。
   - 示例 1（商品说明书）：优利德 UT890D+ 真有效值数字万用表
   - 示例 2（电子设备）：泰克 MSO2000B 系列混合信号示波器
   - 示例 3（工控硬件）：西门子 SIMATIC S7-1200 可编程控制器
   - 示例 4（IT 与网络）：思科 Catalyst 9300 系列企业级交换机
3. 类型适配：不同文档类型的主题名提取方式如下。
   - 科学论文：提取研究对象、核心方法、模型名或数据集名，例如：BGE-M3 多功能文本嵌入模型。
   - 标准规范：提取标准名、规范名或协议名，例如：GB/T 35273 信息安全技术 个人信息安全规范。
   - 技术方案：提取系统名、项目名或建设对象，例如：智慧园区视频监控系统建设方案。
   - 合同文档：提取合同事项或项目名称，例如：企业知识库平台建设项目。
   - 教程文档：提取核心知识主题，例如：LangGraph 多节点工作流开发教程。
   - 报告文档：提取报告分析对象或主题，例如：新能源汽车行业市场分析报告。
4. 降级策略：如果上下文中没有完整名称，但能判断主题，请提取最核心、最具体的短语。
   - 示例：只看到“数字万用表使用说明”，可输出：数字万用表。
   - 示例：只看到“文档切分策略说明”，可输出：文档切分策略。
5. 纯净输出：你是一个接口，只允许返回 JSON，不要输出解释、问候语、Markdown 或多余文本。
6. 防护机制：如果提供的上下文中完全无法识别出任何具体主题，请将 theme_name 严格设置为单词 UNKNOWN。
"""

# 用户提示词模板：用于注入动态变量
THEME_NAME_USER_PROMPT_TEMPLATE = """请分析以下文档信息，并严格按照规则提取文档主题名：

【文档标题】
{file_title}

【文档内容切片】
{context}

请严格返回如下 JSON 格式：
{{
  "theme_name": "文档主题名"
}}"""


DOCUMENT_IDENTITY_SYSTEM_PROMPT = """你是文档编目与身份抽取专家。
你需要区分“文档的正式标题”和“文档主要讨论的对象”，不得将文件名、研究主题和文档标题混为一个字段。

规则：
1. canonical_title 表示这份文档的标题；primary_subject 表示文档主要围绕的设备、论文对象、项目、技术或问题。
2. 只能使用输入中已出现的名称、型号和事实，不得猜测品牌、年份、版本或作者。
3. evidence 必须返回 1–3 个能在输入原文中直接找到的短语，用于支持 canonical_title 或 primary_subject。
4. aliases 只收录原文中真实出现的简称、别名或型号表达。
5. confidence 是 0–1 小数；证据不足时必须降低置信度，不得使用虚构名称补齐。
6. 只返回合法 JSON，不要输出 Markdown 或解释。
"""


DOCUMENT_IDENTITY_USER_PROMPT_TEMPLATE = """请根据以下证据生成文档档案。

【文件名】
{file_title}

【标题候选】
{title_candidates}

【文档内容】
{context}

请严格返回：
{{
  "canonical_title": "文档标题",
  "primary_subject": "主要对象或主题",
  "aliases": ["原文中出现的别名"],
  "document_type": "论文/产品说明书/技术报告/标准规范/教程/其他",
  "summary": "不超过200字的文档摘要",
  "evidence": ["可在上文直接找到的证据短语"],
  "confidence": 0.0
}}"""


FORMULA_SEMANTIC_SYSTEM_PROMPT = """你是一名科研公式信息抽取助手。
你的任务是根据文档上下文解释已经给出的 LaTeX 公式，不得补写上下文中不存在的公式、变量含义、单位、结论或适用条件。
如果上下文无法确定某项信息，对应字段返回空字符串或空列表。只返回合法 JSON，不要输出 Markdown 或解释文字。
"""


FORMULA_SEMANTIC_USER_PROMPT_TEMPLATE = """请批量分析以下文档公式。

【文档主题】
{theme_name}

【待分析公式及各自上下文】
{formulas_json}

每个公式对象中的 section_title 和 context 只用于理解对应公式。不同公式可能来自不同章节，不得混用上下文。

请严格按照以下 JSON 结构返回，并保持 formula_id 不变：
{{
  "formulas": [
    {{
      "formula_id": "输入中的公式ID",
      "description": "公式用途或自然语言含义",
      "variables": [
        {{"symbol": "变量符号", "meaning": "变量含义", "unit": "单位"}}
      ],
      "conditions": ["适用条件或约束"]
    }}
  ]
}}
"""
