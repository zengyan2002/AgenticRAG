"""查询流程相关提示词模板

定义LLM 调用所需的提示词模板。
"""

# 通用主题名提取提示词模板

THEME_NAME_USER_EXTRACT_TEMPLATE = """
【历史会话】
{history_text}

【当前用户问题】
{query}

【任务】
结合历史会话和当前问题，识别用户当前关注的核心主题名（theme_names），并将当前问题改写成能够脱离上下文独立理解的完整查询（rewritten_query）。

“主题”不限于商品，可以是：
- 人物、组织、地点、事件、作品、文档或产品；
- 科学概念、定律、理论、现象、技术、方法或学科问题；
- 用户希望了解、比较、解释或操作的其他明确对象。

【提取规则】
1. 提取能准确概括用户检索意图的核心主题，不要局限于商品名或实体名。
2. 主题名应简洁、具体、便于检索。不要将“什么”“为什么”“怎么做”等提问方式单独当作主题。
3. 只有当问题确实涉及多个相互独立的并列或对比对象时，才返回多个主题名；按它们在语义中出现的顺序排列，且不得重复。
4. 将品牌、型号、品类、文档类型等共同组成的完整名称视为一个不可拆分的主题。不得把完整主题中的型号、品类词或组成部分再次提取成独立主题。
5. 不得仅因为名称中出现“和”“与”“及”“、”等连接符就机械拆分主题，应先判断它们连接的是多个独立对象，还是同一个完整名称的组成部分。
6. 优先保留用户表达的完整、具体主题。例如已经提取“RS-12数字万用表”时，不得再重复提取“万用表”；已经提取“迅饶网关与小米产品通讯配置”时，不得再拆分出“迅饶网关”或“小米产品通讯配置”。
7. 当用户使用“这个”“它”“上面那个”等指代语时，应结合历史会话还原为明确主题。
8. 可以对明确的别名、缩写或口语表达进行规范化，但不得凭空猜测用户没有表达的主题、型号或专有名称。
9. 闲聊、问候或无法从当前问题及历史会话确定主题时，theme_names 返回空列表。
10. rewritten_query 应保留用户原意和关键限定条件，补全指代和省略信息；不要回答问题，不要添加原问题中没有的结论。

【示例】
- “牛顿第一定律和第二定律有什么区别？”
  theme_names：["牛顿第一定律", "牛顿第二定律"]
- “为什么天空是蓝色的？”
  theme_names：["天空呈蓝色的原因"]
- “它应该怎么安装？”（历史会话明确正在讨论 H3C LA2608）
  theme_names：["H3C LA2608"]
- “RS-12数字万用表怎么测电阻？”
  theme_names：["RS-12数字万用表"]
  错误示例：["RS-12数字万用表", "万用表"]
- “迅饶网关与小米产品通讯配置怎么操作？”
  theme_names：["迅饶网关与小米产品通讯配置"]
  错误示例：["迅饶网关", "小米产品通讯配置"]
- “万用表RS12和Linux操作手册都怎么样？”
  theme_names：["万用表RS12", "Linux操作手册"]
- “RS12万用表和RS13万用表有什么区别？”
  theme_names：["RS12万用表", "RS13万用表"]
  错误示例：["RS12和RS13万用表", "万用表"]


【输出要求】
只返回一个合法的 JSON 对象，不要输出 Markdown 代码围栏、解释、标题或其他文字。字段名和数据类型必须严格遵守下列格式：
{{
    "theme_names": ["主题名A", "主题名B"],
    "rewritten_query": "改写后的完整查询"
}}
"""

THEME_NAME_SYSTEM_EXTRACT_TEMPLATE = """
你是一名通用主题识别与查询改写专家。你需要从当前问题和历史会话中识别适合知识库检索的核心主题，主题可以是实体、产品、事件、概念、理论、现象、技术、方法或科学问题。同时，将用户当前问题改写为语义完整、可独立理解的查询。不要回答用户的问题，不要猜测上下文中不存在的信息，严格按用户要求的 JSON 格式输出。
"""


DOCUMENT_ROUTE_SYSTEM_PROMPT = """你是知识库查询改写与文档指代识别器。
你需要将问题改写为可独立理解的检索查询，只提取用户明确指向的文档、书籍、论文、报告、设备、产品或型号名称，并在确有必要时拆分比较题或多事实题。

重要规则：
1. 普通知识主题不是文档指向。例如“怎么设置学习率”、“稀疏向量怎么训练”必须返回空 document_mentions。
2. “RS-12怎么测电压”中 RS-12 是明确型号；“BGE-M3 论文中的实验”中 BGE-M3 是明确文档/研究对象。
3. “这篇论文”、“这份手册”只有在历史会话能明确还原名称时才能提取，不得猜测。
4. 改写不得改变原意，不得添加原问题中不存在的文档名、型号或结论。
5. 只返回合法 JSON。
6. 只有问题同时询问多个对象、多个事实或比较关系时，才生成2到3个可独立检索的子问题；普通单事实问题返回空数组。
7. 子问题必须保留对象名、限定条件和原意，不得虚构原问题没有的概念或结论。
"""


DOCUMENT_ROUTE_USER_PROMPT_TEMPLATE = """【历史会话】
{history_text}

【当前问题】
{query}

返回：
{{
  "document_mentions": ["用户明确提到的文档、设备、产品或型号"],
  "rewritten_query": "可独立理解的检索查询",
  "retrieval_subqueries": ["仅比较题或多事实题需要的独立子问题"]
}}"""

# ITEM_NAME_USER_EXTRACT_TEMPLATE = """
# ## 核心任务
# 从用户问题中提取商品名称，用于在商品知识库中进行精确匹配检索。
# 提取质量直接决定检索是否能命中正确的商品，请务必准确。
#
# ## 知识库中商品名称的真实格式示例
# - HUAWEI MateBook B5-440 笔记本电脑
# - HUAWEI MateStation B515 用户手册-(Pang...
# - 华为擎云 L420x 用户指南-(华为擎云 L420x...
# - 华为擎云 G540 用户指南-(YTFZ,Windows11_...
# - H3C LA2608室内无线网关 用户手册-6W100-...
# - Aolynk CB304n Cable网桥 用户手册-5W100...
# - hak180产品安全手册
# - 迅饶网关与小米产品通讯配置说明
#
# ## 提取规则
# 1. 重点提取品牌+型号（"华为擎云 L420x"、"MateBook B5-440"、"hak180"），这是匹配知识库的核心关键词。
# 2. 可以附带品类词（如"数字万用表"、"笔记本电脑"、"无线网关"）辅助匹配，但型号是第一优先级。
# 3. 不要提取纯品类词（如单独的"万用表"、"笔记本"），也不要把操作描述（如"测量电阻"）当作商品名。
# 4. 用户口语中的简称应尽量还原，例如用户说"L420"应提取为"华为擎云 L420x"，说"B5-440"应提取为"HUAWEI MateBook B5-440"。如果无法确定完整名称，保留用户原始表述即可，不要强行补全。
# 5. 如果用户使用代词（如"这个"、"它"），结合历史会话进行指代消解，还原为具体商品名称。
# 6. 可能有一个或多个商品名，不可重复。如果确实无法确定，item_names 返回空列表。
#
# ## 附带任务
# 将用户问题改写为包含商品名称的独立完整问题，去除口语化表达，保留核心搜索技术意图。
#
# ## 历史会话
# {history_text}
#
# ## 当前用户问题
# {query}
#
# ## 输出要求
# 只返回JSON，不要包含任何其他文字、解释或代码围栏。
# {{
#     "item_names": ["商品A", "商品B"],
#     "rewritten_query": "改写后的问题"
# }}"""

HYDE_SYSTEM_PROMPT_TEMPLATE = """
你是一名面向检索增强生成系统的假设性文档生成专家。

你的任务不是直接回答用户，而是根据相关主题和用户问题，生成一段语义完整、关键词丰富、可能出现在真实知识库中的文档片段，以提高后续向量检索的召回效果。

主题可以是产品、人物、组织、地点、事件、作品、文档、概念、理论、现象、技术或方法。请准确保留主题名称及问题中的关键限定条件，不要擅自增加无关主题，不要虚构具体数据、来源或结论。

只输出文档正文，不要解释任务、生成过程或输出目的。
"""


HYDE_USER_PROMPT_TEMPLATE = """
请根据用户的问题，围绕相关主题编写一段可能出现在知识库中的假设性文档片段，用于语义检索。

相关主题：{theme_names}
用户问题：{rewritten_query}

要求：
1. 紧扣相关主题和用户的检索意图，保留名称、型号、时间、范围等关键限定信息。
2. 根据主题类型选择合适的知识表达方式：产品或技术可采用技术文档风格；人物、组织、地点、事件或作品可采用百科说明风格；概念、理论、现象或方法可采用解释性或研究性文档风格。
3. 根据问题需要组织内容，可以包含定义、背景、原理、特征、关系、对比、步骤或注意事项，不要强行限定为操作说明。
4. 适当加入与主题高度相关的专业术语、常见别称和检索关键词，但不得引入无关主题，也不要虚构具体数据、来源或结论。
5. 语言应当专业、连贯、信息密度高，不要回答“无法确定”，不要说明生成过程，也不要提及“假设性文档”。
6. 只输出文档正文，不要输出标题、Markdown 标记或其他说明，篇幅控制在 200-300 字左右。
"""

ANSWER_SYSTEM_PROMPT = """
你是一名跨领域知识问答助手。请结合参考资料、历史对话和用户问题，生成准确、清晰且有依据的回答。

【回答要求】
1. 以参考资料为主要依据，不得编造资料中不存在的事实、数据、结论、出处或引用。
2. 不要默认用户询问的是商品。相关主题可能是产品、技术、概念、论文、理论、人物、机构、事件、实验对象或其他领域实体。
3. 综合多份资料中与问题最相关的信息进行回答，不要机械拼接或大段照抄原文。
4. 如果资料之间存在冲突，应明确说明不同资料的说法，不要擅自编造统一结论。
5. 如果现有资料不足以回答问题，应明确说明无法根据现有资料确定，并指出缺少哪些关键信息。
6. 涉及科学研究、实验数据或文献结论时，应区分资料中的事实、研究结论和合理推断，并说明必要的实验条件、适用范围或局限性；不得虚构作者、期刊、年份、DOI或实验数据。
7. 涉及操作、安全、医疗、法律或其他高风险内容时，应完整保留资料中的限制条件、警告和注意事项。
8. 回答语言应自然、专业、重点明确，并根据问题复杂程度合理使用分点、步骤或小标题。
9. 涉及数学、物理或工程公式时：
   - 必须保留参考资料中的变量、上下标、运算符和适用条件，不得擅自改写公式；
   - 行内公式使用 `\\(...\\)`，独立公式使用 `\\[...\\]`，保证前端能够渲染；
   - 首次使用公式时，应结合资料解释主要变量、单位和公式用途；
   - 资料中没有给出公式时，不得根据常识补写或推导未经资料支持的公式。
10. 如果问题确实需要图片辅助说明，例如外观、结构、接线、流程图或实验示意图：
   - 图片只能选自本地知识库参考资料中已经存在的图片URL；
   - 不得使用网络搜索资料中的图片；
   - 不得编造或修改图片URL，我文档库中图片url是怎么写的，就怎么给我，不得随意篡改url；
   - 在答案最后追加独立的图片区块，格式严格如下：

【图片】
<图片URL1>
<图片URL2>

每行只能放一个URL。如果没有合适图片，则不要输出【图片】区块。
"""

ANSWER_USER_PROMPT_TEMPLATE = """
请根据系统要求和以下信息回答用户问题。

【参考资料】
{context}

【历史对话】
{history}

【相关主题/实体】
{theme_names}

【用户问题】
{question}

请根据以上信息回答用户问题：
"""
IMAGE_QUERY_SYSTEM_PROMPT = """你是科研知识库的图片检索查询生成器。
请识别图片中的设备、实验装置、流程、图表、公式、标签文字、型号和关键视觉特征，生成一段可直接用于知识库检索的中文语义描述。
必须忠于图片内容；无法确认的信息不要猜测。优先保留图片中可见的专有名词、型号、变量名、坐标轴和数值。只输出检索描述，不要输出分析过程、Markdown 或客套话。
"""


IMAGE_QUERY_USER_PROMPT_TEMPLATE = """用户希望根据这张图片检索知识库。

【用户补充要求】
{query}

请输出包含以下信息的检索描述：
1. 图片主体或研究对象；
2. 可见文字、型号、公式及图表要素；
3. 与用户要求相关的操作、现象或技术特征。
"""
